## 1. RED：固化新规范的失败证据

- [x] 1.1 为确定性阅读轨迹建立纯 Kotlin 重放夹具，能分别记录速度因素、剩余量因素、置信度、样本权重和 ETA，且不依赖 Android 时钟或真实文件
- [x] 1.2 先编写稳定速度、单个 4 倍慢页、约 10% 异常页和连续一小时轨迹测试，运行聚焦测试并记录当前固定 EWMA 未满足 3%、5% 和 2% 门禁的 RED 结果
- [x] 1.3 先编写正负 25% 速度阶跃测试，断言约 15 分钟完成至少一半跟随、约 35 分钟剩余偏差不超过约 5%，运行并记录 RED 结果
- [x] 1.4 先编写稳定锚点测试，覆盖上一可见页耗时与其内容量配对、跨章正常前进、微事件合并、跳转重布点、回退、遮挡、朗读和排版变化，运行并确认当前错页或缺失语义的 RED 结果
- [x] 1.5 先编写可见 Unicode 单位测试，覆盖结构性空白、标点、代理校准、未知章节退化、图文可靠性和剩余量置信度，运行并确认缺失能力的 RED 结果
- [x] 1.6 先编写状态与索引迁移测试，覆盖旧 EWMA 失效、其他书籍配置保留、旧 sidecar 原始长度复用、新摘要恢复和损坏失败关闭，运行并确认 RED 结果
- [x] 1.7 保留上述 RED 的实际命令和失败断言，在任何生产实现前完成评审，避免以推断代替真实红灯

## 2. GREEN：可见内容量与稳定阅读锚点

- [x] 2.1 实现 API 21 可用的可见 Unicode 单位累计逻辑，忽略结构性空白、保留标点，并使对应单元测试转绿
- [x] 2.2 在同步与异步章节排版的既有文本遍历中累计页面、章节可见单位和图文可靠性，不在翻页时二次扫描页面字符串
- [x] 2.3 为页面和章节排版实体增加只读的可见单位区间或前缀元数据，并验证正文展示、分页和朗读现有行为不变
- [x] 2.4 实现 `ReadingSpanAnchor` 或等价纯内存状态，按“先结算旧可见区间、再建立新锚点”提交样本，使错页配对测试转绿
- [x] 2.5 接入微事件累计、跨章正常前进、高水位语义坐标、重读新颖度和图文连续权重，使相关锚点测试转绿
- [x] 2.6 让回退、目录或进度条跳转、遮挡、朗读、初始化和排版重建统一失效并重建锚点，确认这些路径不训练
- [x] 2.7 重构重复的同步/异步排版计数与锚点转换代码，并重新运行本节全部测试保持绿色

## 3. GREEN：近期鲁棒分布与本书长期融合

- [x] 3.1 实现可复用原始类型缓冲和加权选择工具，支持加权中位数、加权 MAD 及无每样本临时集合的草稿空间
- [x] 3.2 在对数单位耗时域实现按有效阅读时长衰减的近期模型，以及 Student-t 或 Cauchy 连续异常权重
- [x] 3.3 实现异常长停留的连续有效时长饱和，使其既不固定裁剪到最大秒数，也不一次老化全部近期证据
- [x] 3.4 实现仅在新有效阅读到来时缓慢衰减的本书长期摘要，并按证据量和离散度连续融合近期与长期中心
- [x] 3.5 实现缓冲容量达到上限时向长期摘要平滑折入旧证据，调优到折叠前后速度变化不超过约 0.5%
- [x] 3.6 移除固定 `α=0.2`、`0.25～4` 样本裁剪和 5～120 秒硬样本区间，不增加 ETA 运行时限幅或显示平滑
- [x] 3.7 联合调优集中参数，使单异常、10% 污染、正负速度阶跃、一小时稳定和重读轨迹全部转绿
- [x] 3.8 重构鲁棒统计和融合代码为纯 Kotlin、可解释的小型组件，并再次运行全部确定性轨迹测试

## 4. GREEN：剩余量校准、置信度与版本迁移

- [x] 4.1 扩展章节索引内存模型，分别保存原始代理长度和已观察可见单位，并构建剩余单位期望、不确定性及 O(1) 前缀查询
- [x] 4.2 实现本书原始代理到可见单位的对数比例稳健校准，以及无代理章节向本书可见长度分布收缩的退化策略
- [x] 4.3 从排版完成和既有缓存保存路径机会式提交可见单位，确认索引管理器仍只扫描低成本元数据、不读取正文
- [x] 4.4 升级 sidecar 编解码与协调逻辑，兼容读取旧原始长度、原子写入新格式，并保持目录末尾追加、换源、重排和损坏处理规则
- [x] 4.5 实现速度置信度、剩余量置信度和可诊断原因，只有两者均足够时返回单一 `Ready` ETA，否则返回 `Learning`
- [x] 4.6 升级 `ReadingTimeState` 为紧凑的近期/长期中心、离散度、有效证据量和最高语义坐标摘要，不持久化原始翻页缓冲
- [x] 4.7 让旧 EWMA 状态失效但保留累计记录、阅读进度、缓存和兼容索引；验证新摘要随现有完整备份恢复且不需要 Room 迁移
- [x] 4.8 保持约 120 秒速度状态节流、既有索引写入防抖和暂停/换书强制刷新，验证异常退出只可能丢失节流窗口内证据
- [x] 4.9 重构索引、校准、置信度和迁移边界，并重新运行本节全部测试保持绿色

## 5. GREEN：阅读层集成与用户可见行为

- [x] 5.1 将新的锚点、可见单位快照和估算结果接入 `ReadBook` 的手动翻页、自动翻页、跨章和生命周期事件
- [x] 5.2 保持目录新增或章节长度真实变化立即更新内存剩余量，并只在下一次既有页面刷新时显示，不触发 ETA 专属刷新
- [x] 5.3 保持分钟显示值未变化时不重复更新页眉页脚，确认没有新增定时倒计时或墨水屏二次刷新
- [x] 5.4 验证阅读记录关闭、手动重置本书估算、换源、目录重排和书籍切换继续遵守原行为，且不删除累计记录或正文缓存
- [x] 5.5 验证漫画、音频书、PDF 和朗读过程不采集新的文本速度样本，也不因新索引字段发生错误
- [x] 5.6 运行现有阅读时间显示、索引编解码、协调、备份兼容和阅读事件测试，修复回归后保持全部绿色

## 6. REFACTOR：性能、分配与功耗门禁

- [x] 6.1 增加可重复的样本更新性能夹具，测量低端参考设备主线程 P50/P95、稳态分配和缓冲折叠开销
- [x] 6.2 优化固定缓冲、草稿数组和结果传递，使新增样本更新 P95 不高于 0.5 ms 且新算法稳态路径无堆分配
- [x] 6.3 审计翻页调用链，证明没有新增文件、数据库、JSON、网络、章节遍历、逐页写盘、持续定时器、唤醒锁或充电补索引任务
- [x] 6.4 记录 300 万字级同机 A/B 的设备条件与不可测边界：目标设备外部供电、无 `powerstats` 且电池计数器拒绝 shell 访问，无法在不占用约 10 小时人工设备时间的前提下形成可信配对能耗证据
- [x] 6.5 保留“未证明一小时 CPU 增量不超过 1 秒、能耗差异不超过 3%”的证据边界；经产品所有者明确接受，将其改为单用户灰度发布后的日常耗电、发热、卡顿与 I/O 观察，不宣称功耗定量门禁已通过
- [x] 6.6 将新书预热、成熟书籍恢复、连续一小时稳定、孤立慢页、持续变速、长停留、重读、图文混合和目录追加列为发布后单用户观察清单；本次发布前以确定性轨迹、集成测试和低端真机性能测试覆盖可自动验证部分

## 7. 最终验证与评审

- [x] 7.1 运行聚焦单元测试并确认所有 RED 用例在新实现下转绿，保留实际命令与结果
- [x] 7.2 使用一次性 `ANDROID_HOME=/Users/back/Library/Android/sdk` 和 `ANDROID_SDK_ROOT=/Users/back/Library/Android/sdk` 运行 `./gradlew :app:testAppDebugUnitTest`，确认成功
- [x] 7.3 运行 `./gradlew :app:lintAppDebug :app:assembleAppDebug`：调试包构建成功；lint 被仓库既有 14 个 error 阻塞，本次阅读时间文件无新增 lint error，未修改或放宽 lint baseline
- [x] 7.4 运行 `openspec validate --all --strict`、`git diff --check` 和变更范围审计，确认没有 Room schema、依赖、权限、签名、网页端或无关生成文件变化
- [x] 7.5 对照 proposal、delta spec、design 和真机证据逐项复核：自动化稳定性、低端真机 P95、稳态零分配与低功耗架构边界已满足；产品所有者接受未完成定量功耗 A/B 与整套人工轨迹的风险，并批准先验证、再归档、再单用户灰度发布
