## 1. 清晰模式可行性门槛

- [x] 1.1 实现不依赖厂商接口的最小可复用宽幅空间交叉淡变绘制单元，使灰阶叠印只来自新旧页面内容且不绘制独立灰条；根据实机反馈保持扫动位置按时间线性跨越视口，仅将交叉区内容混合修订为首尾较缓、中段较快的非线性灰阶比例，并补充方向、交叉区位置、内容混合比例和时间进度的聚焦测试。
- [x] 1.2 增加仅用于 `appDebug` 的最小实机验证入口，使用固定文字页面依次运行 120、160、200、220、250ms 的前进和后退动画，不接入海信 Binder、隐藏 API、sysfs 或全局刷新模式切换。
- [x] 1.3 更正首轮至第三轮的刷新模式证据：用户复核确认相关可见动画实际来自流畅模式；补记极速、流畅、均衡和清晰模式的动画、卡顿、残影与最终清晰度结果，不再把既有录像标为清晰模式证据。
- [x] 1.4 将普通 Canvas 路线记录为 `NO_GO_CLEAR_AND_ANIMATED`：清晰模式基本无动画，非清晰模式存在残影；撤回旧 `GO` 和 220ms 最终参数，200ms 仅保留为极速模式软件候选。
- [x] 1.5 只读审计 Hi Reader Pro framework 的自动组合刷新流程，确认只有标准纵向滚动会进入动态刷新并在滚动结束后恢复模式；横向触摸不会触发，且该流程不提供 Kindle 的方向与步数波形接口。
- [x] 1.6 在仅用于 `appDebug` 的固定文字页执行有界组合刷新实验：真实横向滑动未触发动态刷新；标准事件桥接发生递归并短暂改变刷新状态，记为 `NO_GO_UNSAFE_EVENT_BRIDGE`，代码已移除，设备动态标志已由 framework 正常恢复为 `0`。

## 2. 动画协议、设置与本地化

- [x] 2.1 在 `PageAnim` 尾部追加协议值 `5` 并更新注解范围，补充既有 `0..4` 含义不变和未知值仍降级为无动画的兼容测试。
- [x] 2.2 将新值接入 `ReadView` 的 Delegate 选择，并确认图片书、滚动模式、自动阅读和非相邻跳转的既有选择逻辑不被改变。
- [x] 2.3 在普通阅读、墨水屏阅读和单本书覆盖设置末尾追加“Kindle翻页动画”，保持列表下标与持久化值一致，为现有八套语言资源提供自然本地化名称，并让宽度不足的设置按钮保持单行尾部省略。
- [x] 2.4 补充全局阅读配置 JSON 和单本书阅读配置 JSON 的读取、写入与旧配置回归测试，确认无需 Room 列、schema 快照或迁移变化。

## 3. 生产动画与连续输入状态

- [x] 3.1 以普通屏幕及快速刷新模式的软件候选参数实现生产 Delegate：移动阶段仅确认手势，松手后按方向播放；横屏双页作为完整视口绘制，阅读内容参与动画而系统栏和弹窗不参与；该实现不作为清晰模式门槛通过证据。
- [x] 3.2 实现可独立测试的 `Idle`、`Animating`、`Returning`、`Committing` 状态及 generation 失效保护，保证同向输入逐页快进、反向输入净抵消、每页最多提交一次、边界与加载失败清空待处理意图。
- [x] 3.3 仅为新动画统一点击、音量键和键盘的离散翻页入口：绕过现有 600ms 丢弃式防抖，长按关闭时忽略 repeat，长按开启时限制为最快约每最终动画时长一页；其他动画行为保持不变。
- [x] 3.4 用户主动选择新模式后不受设备系统动画时长比例为 `0` 的影响；在动画入口处理视口无效、录制或绘制失败、生命周期销毁和方向变化，统一降级为正确最终页或原页，不安排 500ms 补刷。
- [x] 3.5 移除清晰模式调试入口和候选参数切换代码，只保留选定的生产参数、可复用绘制代码及必要测试夹具。
- [ ] 3.6 `NOT_RUN_PROTOCOL_STOP`：任务 1.6 未通过，不得把组合刷新桥接接入点击、滑动、音量键和键盘；如需继续，必须先明确批准海信私有接口或更高权限的范围扩张并重新设计。

## 4. 聚焦自动化与普通屏幕验证

- [x] 4.1 为状态控制器补充连续二十次同向输入、动画中反向抵消、章节或书籍边界、旧 generation 回调、失败降级和单次提交计数测试。
- [x] 4.2 为手势补充未越阈值取消、松手后才启动、前后方向和目标页缺失场景；为按键补充独立快速按下不丢页及长按限速场景。
- [ ] 4.3 在普通 Android 屏幕人工验证竖屏、横屏双页、浅色、深色、少量插图和跨章节；确认系统动画时长比例为 `0` 时主动选择的新模式仍播放，非相邻跳转不播放，并记录实际结果。

## 5. Hi Reader Pro 最终实机验收

- [x] 5.1 仅安装或更新 `io.legado.app.debug`，确认未对 `io.legado.app.release` 执行覆盖、卸载、清数据或降级；使用独立调试数据和可公开测试文本。
- [ ] 5.2 `NO_GO_CLEAR_AND_ANIMATED`：清晰模式基本看不到动画，非清晰模式存在残影，尚未满足前进、后退、横屏双页、浅色和深色主题均“扫动可见且最终清晰”的物理屏幕门槛。
- [ ] 5.3 `NOT_RUN_PROTOCOL_STOP`：任务 5.2 未通过，不继续用二十次翻页、反向抵消或按键验收包装未达成的核心硬件效果；状态机与限速语义仅保留自动化证据，设备验收等待重新批准硬件范围后执行。
- [x] 5.4 审计 APK 和代码差异，确认不包含提取的海信 framework 文件、`com.hmct.epd` 调用、隐藏 API、sysfs、root、系统签名、全局刷新模式切换或 500ms 延迟补刷。

## 6. 最终项目检查

- [x] 6.1 运行 `./gradlew :app:testAppDebugUnitTest`，修复本变更引入的失败并记录真实结果。
- [ ] 6.2 `BASELINE_LINT_BLOCKED`：`:app:assembleAppDebug` 成功且本变更新增文件没有 lint 错误，但 `:app:lintAppDebug` 仍因项目既有 14 个错误失败；未经独立授权不修改 lint 基线或顺手修复无关错误。
- [x] 6.3 运行 `git diff --check`、检查 `git diff --stat`，确认没有 Room schema、依赖升级、生成物、设备私有文件或无关格式化差异。
- [x] 6.4 运行 `openspec validate --all --strict`，核对提案、规范、设计、任务与实际 GO/NO-GO 及实机验证状态一致；未执行或协议停止的项目不得标记完成。
